# TODO

- [ ] Fix robusto de `getUserConversations` para que no falle si `chats` tiene `participants` incompleto o con tipo inesperado.
- [ ] (Opcional) Log/debug del error real en UI para detectar rápidamente fallos de Firestore.
- [ ] Verificar que el chat lista aparece y que abre `ConversationScreen` correctamente.

